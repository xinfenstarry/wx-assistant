# 隐私说明（开发版）

## App 会读取什么

- 微信通知中的会话标题、发送者、正文和通知时间；
- 用户明确选择的微信群在当前屏幕向 Android 无障碍系统暴露的可见文字；
- 用户配置的 DeepSeek API Key（仅用于调用 DeepSeek）；以及 DeepSeek 返回的摘要和结构化计划结果。

## 用途

数据仅用于：去重保存已选群消息、生成发送给 DeepSeek 的提示、展示摘要和计划、比较计划变化、发送本机计划变更通知。

## 数据分享

用户保存 API Key 并在预览页确认整理后，App 才会通过 HTTPS 将所选群消息、当前计划和提示词发送到 `api.deepseek.com` 的 `deepseek-v4-flash`。请求使用用户自己的 DeepSeek 账户；App 不会把 Key 或消息发送到其他服务，也不会向微信发送消息。

## 存储与删除

- 数据保存在 App 私有 SQLite 数据库和私有偏好设置中；
- DeepSeek API Key 使用 Android Keystore 加密后保存，不写入普通偏好、日志或 APK；
- 消息默认保留 30 天，并在 App 启动时清理；
- 用户可在首页“清空全部本地数据”；
- `android:allowBackup="false"`，避免常规系统备份带走数据库。

## 无障碍服务

无障碍服务仅用于微信只读采集。它不会输入、粘贴、回复或发送微信消息，不会更改系统设置，也不会阻止用户关闭服务或卸载 App。扫描导航必须由用户在 App 内发起，并受一次性短时请求约束。

## 不会读取或保存

- 未选中会话的消息正文；
- 密码输入框；
- 微信语音通话/音频；
- 通讯录、相册或精确位置；
- DeepSeek 账户密码、登录 Cookie 或其他账户凭据（仅保存用户主动输入的 API Key）。

正式发布时，开发者仍需补充主体信息、联系邮箱、适用地区、第三方服务条款和可公开访问的隐私政策 URL。
